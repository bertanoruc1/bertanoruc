// defineNuxtConfig fonksiyonunu doğrudan kullanıyoruz. 
// Bu, Nuxt CLI tarafından global olarak enjekte edildiği için modül hatasını çözer.
export default defineNuxtConfig({
  // Başka bir Nuxt yapılandırması gerekiyorsa buraya ekleyin (örneğin modules, css, app, runtimeConfig)
  // Vite yapılandırması kaldırıldı.
})